import type {License} from "../../src/features/licenses/types";
export function LicenseList({licenses: _licenses}:{licenses:License[]}){
 // TODO(level-02): Type the prop as licenses, render one row per license, and conditionally show expiry.
 return <p>Licenses will appear here.</p>;
}
