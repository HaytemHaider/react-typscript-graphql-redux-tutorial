import{render,screen}from"@testing-library/react";import{describe,it,expect}from"vitest";import{FluentLicenseList}from"./FluentLicenseList";
const data=[{id:"1",organizationId:"o",productName:"Camera",status:"active" as const}];describe("Fluent license controls",()=>{it("provides an accessible search control",()=>{render(<FluentLicenseList licenses={data}/>);expect(screen.getByRole("textbox",{name:/search licenses/i})).toBeVisible()});it("offers an add action",()=>{render(<FluentLicenseList licenses={data}/>);expect(screen.getByRole("button",{name:/add license/i})).toBeEnabled()});it("exposes status text",()=>{render(<FluentLicenseList licenses={data}/>);expect(screen.getByText("active")).toBeVisible()})});
