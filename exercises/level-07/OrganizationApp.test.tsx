import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{describe,it,expect}from"vitest";import{OrganizationApp}from"./OrganizationApp";describe("organization state",()=>{it("initially displays Acme licenses only",()=>{render(<OrganizationApp/>);expect(screen.getByText("Camera")).toBeVisible();expect(screen.queryByText("Entry")).not.toBeInTheDocument()});it("switches licenses through the UI",async()=>{render(<OrganizationApp/>);await userEvent.selectOptions(screen.getByRole("combobox"),"org-2");expect(screen.getByText("Entry")).toBeVisible();expect(screen.queryByText("Camera")).not.toBeInTheDocument()})});
