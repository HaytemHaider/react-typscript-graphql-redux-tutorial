import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{createMemoryRouter,RouterProvider}from"react-router-dom";import{describe,it,expect}from"vitest";import{Layout,LicenseLinks,Details}from"./App";
const make=()=>createMemoryRouter([{path:"/",element:<Layout/>,children:[{path:"licenses",element:<LicenseLinks/>},{path:"licenses/:licenseId",element:<Details/>}]}],{initialEntries:["/licenses"]});describe("license routes",()=>{it("navigates to a license id",async()=>{const router=make();render(<RouterProvider router={router}/>);await userEvent.click(screen.getByRole("link",{name:"Camera Station"}));expect(router.state.location.pathname).toBe("/licenses/lic-123")});it("shows parameterized details",async()=>{render(<RouterProvider router={make()}/>);await userEvent.click(screen.getByRole("link",{name:"Camera Station"}));expect(screen.getByRole("heading",{name:/license details/i})).toBeVisible();expect(screen.getByText(/lic-123/)).toBeVisible()})});
