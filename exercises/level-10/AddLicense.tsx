export function AddLicense(_props:{onSubmit?:(value:{productName:string;licenseKey:string})=>void;error?:string}){
 // TODO(level-10): Build the Fluent Dialog form and execute the mutation.
 return <button>Add license</button>;
}
