import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{describe,it,expect,vi}from"vitest";import{AddLicense}from"./AddLicense";describe("add license",()=>{it("opens an accessible dialog",async()=>{render(<AddLicense/>);await userEvent.click(screen.getByRole("button",{name:/add license/i}));expect(screen.getByRole("dialog",{name:/add license/i})).toBeVisible()});it("prevents an empty submission",async()=>{render(<AddLicense/>);await userEvent.click(screen.getByRole("button"));expect(screen.getByRole("button",{name:"Add"})).toBeDisabled()});it("submits valid values and closes",async()=>{const submitted=vi.fn();render(<AddLicense onSubmit={submitted}/>);await userEvent.click(screen.getByRole("button"));await userEvent.type(screen.getByRole("textbox",{name:/product name/i}),"Camera");await userEvent.type(screen.getByRole("textbox",{name:/license key/i}),"ABC");await userEvent.click(screen.getByRole("button",{name:"Add"}));expect(submitted).toHaveBeenCalledWith({productName:"Camera",licenseKey:"ABC"});expect(screen.queryByRole("dialog")).not.toBeInTheDocument()});it("keeps a server error visible",async()=>{render(<AddLicense error="Could not add license"/>);await userEvent.click(screen.getByRole("button"));expect(screen.getByRole("alert")).toHaveTextContent(/could not add/i)})});
