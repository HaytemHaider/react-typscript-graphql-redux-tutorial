import { NavLink, Outlet } from "react-router-dom";
import { makeStyles, tokens } from "@fluentui/react-components";
import { OrganizationSelector } from "../features/organizations/OrganizationSelector";
const useStyles=makeStyles({root:{minHeight:"100vh",backgroundColor:tokens.colorNeutralBackground2},header:{display:"flex",justifyContent:"space-between",alignItems:"center",padding:tokens.spacingHorizontalL,backgroundColor:tokens.colorBrandBackground,color:tokens.colorNeutralForegroundOnBrand},body:{display:"grid",gridTemplateColumns:"12rem 1fr"},nav:{display:"flex",flexDirection:"column",gap:tokens.spacingVerticalM,padding:tokens.spacingHorizontalL},main:{padding:tokens.spacingHorizontalXXL}});
export function AppLayout(){const s=useStyles();return <div className={s.root}><header className={s.header}><strong>Mini Systems</strong><OrganizationSelector /></header><div className={s.body}><nav className={s.nav} aria-label="Main navigation"><NavLink to="/">Dashboard</NavLink><NavLink to="/organizations">Organizations</NavLink><NavLink to="/licenses">Licenses</NavLink><NavLink to="/products">Products</NavLink><NavLink to="/settings">Settings</NavLink></nav><main><Outlet /></main></div></div>}
